// app/(public)/terms/page.tsx — Sunrise-tracked; reduced to a re-export of app-owned content.
// app:shim — replaced by app-owned content; keep this line on upstream merges ("keep mine").
export { default, metadata } from '@/components/app/marketing/terms-page';
